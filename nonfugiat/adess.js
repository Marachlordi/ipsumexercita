const itemHandler = {
    grabItem: function(e) {
        console.log('Item grabbed!', e);
        // Function body: Perform actions to handle the grabbed item
    }
};

// Usage:
itemHandler.grabItem(event);
